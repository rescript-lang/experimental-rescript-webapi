type htmlElement = private {}
type node = private {}
type nodeList<'tNode> = private {}
type htmlCollection<'t> = private {}
type shadowRoot = private {}
type htmlSlotElement = private {}

type domTokenList = {
  length: int,
  mutable value: string,
}

type namedNodeMap = private {
  length: int,
}

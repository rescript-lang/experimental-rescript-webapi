let location = WebApi.DOM.Global.document.location

// Access properties using `.`
let href = location.href

// Invoke methods using the `->TypeModule`
location->WebApi.DOM.Location.reload

let a = 0
